// ─── 2. Tuple for Coordinates [x, y] ─────────────────────
const coordinates: [number, number] = [40.7128, -74.006];
const [x, y] = coordinates;
 
console.log("\nCoordinates:", coordinates);
console.log("X:", x, "| Y:", y);